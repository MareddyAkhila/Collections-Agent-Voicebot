{
  "type": "object",
  "properties": {
    "account_id": {
      "type": "string",
      "description": "The customer's account ID, e.g. ACC-88392"
    },
    "channel": {
      "type": "string",
      "enum": ["SMS", "WhatsApp", "BOTH"],
      "description": "The channel to send the payment link through."
    }
  },
  "required": ["account_id", "channel"]
}
